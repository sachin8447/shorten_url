server:
  port: 8447
  servlet:
    context-path: /short


spring:
  data:
    mongodb:
      database: admin
      port: 27017
      host: localhost
  application:
    name: SHORTEN-URL
    zipkin:
      base-url: http://127.0.0.1:9411/




eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone : http://localhost:8761/eureka/
  instance:
    hostname: localhost